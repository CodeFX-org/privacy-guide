<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
				android:layout_width="fill_parent"
				android:layout_height="?android:attr/listPreferredItemHeight"
				android:padding="6dip"
				android:background="@drawable/app_item">

	<ImageView
		android:id="@+id/appIcon"
		android:layout_width="wrap_content"
		android:layout_height="fill_parent"
		android:layout_alignParentBottom="true"
		android:layout_alignParentTop="true"
		android:layout_marginEnd="6dip"
		android:contentDescription="@string/localizedAppIcon_contentDescription"
		android:src="@drawable/ic_launcher"/>

	<TextView
		android:id="@+id/appName"
		android:layout_width="fill_parent"
		android:layout_height="32sp"
		android:layout_alignParentTop="true"
		android:layout_alignParentEnd="true"
		android:layout_toEndOf="@id/appIcon"
		android:gravity="center_vertical"
		android:text="@string/localizedAppName_defaultText"
		android:textSize="20sp"/>

	<TextView
		android:id="@+id/appDescription"
		android:layout_below="@id/appName"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:layout_alignParentEnd="true"
		android:layout_alignParentBottom="true"
		android:layout_toEndOf="@id/appIcon"
		android:gravity="center_vertical"
		android:ellipsize="marquee"
		android:singleLine="true"
		android:text="@string/localizedAppDescription_defaultText"
		android:textSize="14sp"/>

</RelativeLayout>